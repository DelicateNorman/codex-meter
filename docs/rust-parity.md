# Rust rewrite parity checklist

This document is the acceptance specification for rewriting Codex Meter 0.15.0 in Rust. The Python implementation at tag `v0.15.0` is authoritative unless a later product decision explicitly changes behavior. Every item begins as `TODO`; an item may be marked complete only after its stated Rust verification passes.

## Verification snapshot (2026-08-12)

The `rust-rewrite` branch is feature-complete as a v0.16 alpha candidate. The
checkboxes below remain the stricter stable-release gate; they are deliberately
not mass-checked merely because the implementation compiles.

| Area | Evidence recorded for the alpha candidate |
|---|---|
| Command and data surface | All 28 Python top-level commands and their nested subcommands/options are present in the Rust parser. Empty-state command smoke tests pass on every supported target. |
| Rust tests | 76 tests pass with both current stable Rust and the declared minimum Rust 1.85 toolchain. `cargo fmt --check` and strict Clippy (`-D warnings`) pass. |
| Python regression | All 71 Python tests pass while the Rust candidate is present, including the automated Python/Rust database and JSON/CSV export differential fixture. |
| Full-history differential | A 0.63 GiB local history (60 Rollouts) produced exactly 1,712 turns, 20,643 LLM calls, 1,926 tools, 12 model groups, 50 sessions, 13 project groups, and matching exports in both implementations. |
| Existing-home compatibility | A backup of the populated v0.15 home opened without migration loss; all read/report/export commands passed, table counts were unchanged, and SQLite integrity remained `ok`. |
| Protocols and privacy | Real loopback OTLP logs/metrics/traces, App Server ingest/stdio proxy, HTTP and TLS reverse proxy, CONNECT, and TLS probe paths passed. Canary prompts, responses, headers, credentials, tool bodies, and proxy bodies were absent from SQLite, WAL, and persisted files. |
| Interactive behavior | A real Linux PTY verified non-blocking first paint, quota/remote redraw, refresh, slash/project modal semantics, recent-project ordering, Unicode input, short layouts, and terminal restoration. |
| Performance | The optimized Linux executable is about 5.7 MiB. The full first import measured 2.10 s / 46 MiB versus Python's 3.51 s / 73 MiB; unchanged startup was below 10 ms / 5 MiB versus about 90 ms / 22 MiB. |
| Native CI | [GitHub Actions run 31593586758](https://github.com/DelicateNorman/codex-meter/actions/runs/31593586758) passed formatting, Clippy, the then-current 61-test suite, release build, and command smoke on Linux x86_64, macOS arm64, macOS x86_64, and Windows x86_64. Linux also ran the cross-implementation differential test. The 76-test final audit suite is covered by the subsequent branch CI run. |

Still reserved for the stable-release gate: installer/checksum conversion and a
tagged artifact dry run; a real external SSH alias (none is configured on the
verification host); privileged live `tcpdump` capture (the parser and safe
failure path are covered); and recorded manual Windows-console/WSL interaction.
The alpha branch therefore does not replace `main`, alter the stable installer,
or create a release tag.

## Parity rules

- [ ] **TODO — P-001: No feature deletion.** The Rust binary must preserve every command, option, interactive view, collector, query, privacy guarantee, and supported platform listed below. Authority: `codex_meter/cli.py:build_parser`, `README.md`, and `CHANGELOG.md`. Rust verification: compare `codex-meter --help` and every nested `--help` against a v0.15.0 Python reference; run the complete checklist on release binaries.
- [ ] **TODO — P-002: Existing data remains usable.** The Rust binary must open and update a Python-created `meter.db`, `config.toml`, and `pricing.json` without resetting, deleting, or silently reinterpreting history. Authority: `codex_meter/config.py:initialize_home`, `codex_meter/storage.py:Storage.migrate`, and `codex_meter/migrations/*.sql`. Rust verification: copy a populated v0.15.0 home, run all read commands and one incremental import, then compare table counts and aggregate outputs before/after.
- [ ] **TODO — P-003: Metadata-only privacy is an invariant.** Prompts, responses, reasoning text, tool output, request/response bodies, packet payloads, arbitrary headers, credentials, authorization values, and email addresses must never be persisted. Authority: module docstrings in `models.py`, `session_jsonl.py`, `otlp_http.py`, `app_server.py`, `network.py`, `proxy.py`; `tests/test_collector.py:test_collector_never_persists_payload_text`; `tests/test_live_observability.py`. Rust verification: seed a unique secret into every supported input surface, exercise all collectors/proxies, scan the database, WAL/SHM, files and logs byte-for-byte, and assert the secret is absent.
- [ ] **TODO — P-004: Data quality remains explicit.** Preserve `data_source`, `confidence` (`exact`, `derived`, `estimated`, `unknown`) and `estimated` fields, including exact App Server/raw-response usage versus derived Rollout cumulative deltas. Authority: `codex_meter/models.py:Confidence`, `Quality`, and record defaults. Rust verification: fixture assertions on every normalized record and exported row.
- [ ] **TODO — P-005: Cross-implementation differential suite.** The same frozen Rollout, App Server, OTLP, tcpdump and pricing fixtures must produce equivalent normalized rows and user-visible reports in Python 0.15.0 and Rust. Floating timing/cost values may use documented tolerances; IDs, token counts, grouping, deduplication, `N/A` decisions and exit codes must match exactly. Rust verification: automated golden/differential test job.

## Process entry point, paths, and initialization

- [ ] **TODO — E-001: Binary identity.** Expose the executable as `codex-meter`; `--version` prints `codex-meter <version>`, `-h/--help` works, and the program description remains “Local-first Codex usage observability.” Authority: `pyproject.toml:[project.scripts]`, `codex_meter/__main__.py`, `codex_meter/cli.py:build_parser`. Rust verification: CLI snapshot tests.
- [ ] **TODO — E-002: Global options.** Preserve `--home PATH`, `--db PATH`, `--no-color`, and `--version`, including global-option placement before the subcommand. Authority: `codex_meter/cli.py:build_parser`. Rust verification: parser tests and subprocess snapshots for all four options.
- [ ] **TODO — E-003: Home resolution.** Meter home is `CODEX_METER_HOME` when set, otherwise `~/.codex-meter`; Codex home is `CODEX_HOME` when set, otherwise `~/.codex`. `--home` overrides Meter home and `--db` overrides only the SQLite path. Authority: `codex_meter/cli.py:_meter_home`, `_codex_home`, and `main`. Rust verification: isolated environment tests for every precedence combination.
- [ ] **TODO — E-004: First-run files.** Create Meter home and `logs/`, create `config.toml` from the bundled default if absent, copy bundled `pricing.json` only if absent, and default SQLite to `<home>/meter.db`. Existing config and price overrides must not be overwritten. Authority: `codex_meter/config.py:initialize_home`. Rust verification: first-run/idempotency tests in a temporary home.
- [ ] **TODO — E-005: Filesystem privacy.** On POSIX, request mode `0700` for Meter home, `logs/`, and TLS directories, and `0600` for private keys; tolerate platforms/filesystems where chmod is unavailable. Authority: `config.py:initialize_home`, `storage.py:Storage.__init__`, `proxy.py:initialize_tls_material`. Rust verification: Unix permission tests and Windows smoke tests.
- [ ] **TODO — E-006: Default invocation.** With no subcommand, incrementally import local Rollouts. When stdin/stdout are TTYs, open the interactive dashboard immediately and refresh remote history/quota in background; otherwise synchronously sync configured remotes and print today’s overview. Authority: `codex_meter/cli.py:main`, `_interactive_dashboard`. Rust verification: pseudo-terminal and redirected-output integration tests, including a deliberately slow quota/SSH stub proving first paint is non-blocking.
- [ ] **TODO — E-007: Startup persistence setup.** Before ordinary commands, run schema migrations and synchronize the selected price catalog into `pricing_snapshots`. Authority: `codex_meter/cli.py:main`, `storage.py:migrate`, `sync_pricing`. Rust verification: start from empty and each historical migration state, then inspect schema and snapshots.

## CLI command surface

Each command must retain help text intent, defaults, exit-code behavior, stdout/stderr separation, and plain-text columns sufficiently closely for existing scripts.

| Status | Command | Required behavior and options | Python authority | Rust verification |
|---|---|---|---|---|
| TODO | `import [PATH] [--force]` | Default path `$CODEX_HOME/sessions`; accept one Rollout file or recurse for `rollout-*.jsonl`; skip unchanged files unless forced; print files/turns/calls/tools/duplicates/malformed summary; return 1 if any file fails. | `cli.py:_import`; `session_jsonl.py:discover_rollouts` | Fixture tree, changed-file and mixed-failure subprocess tests. |
| TODO | `today [--refresh] [--account LABEL] [--project NAME]` | Today in local time; refresh local and configured remotes when requested; apply optional account/project filters. | `cli.py:build_parser`, `main`; `storage.py:overview` | Golden report and filter tests under fixed timezone. |
| TODO | `summary [--period day\|week\|month\|all] [--date YYYY-MM-DD] [--account LABEL] [--project NAME] [--refresh]` | Default day/today; week is local Monday–Sunday; month is local calendar month; all is since first use; invalid dates return 2 with an error. | `cli.py:_summary`, `_period_bounds` | Boundary tests including leap month/year/week and bad input. |
| TODO | `history [--group day\|week\|month] [--account LABEL] [--project NAME] [--refresh]` | Group all owned usage by local calendar bucket, ascending; show sessions, turns, calls, token columns and cost; print empty-state message. | `cli.py:_history`; `storage.py:usage_history` | Golden grouping tests around UTC/local midnight and year transitions. |
| TODO | `account status` | Show OS username/UID and whether manual labeling is enabled; explain that credentials/auth files are not read. | `cli.py:_account`, `_identity_status` | Subprocess snapshot without reading a canary auth file. |
| TODO | `account enable LABEL` / `account set LABEL` | Enable labeling for future sessions and set a non-secret single-line label. Existing session labels remain unchanged. | `config.py:update_account_identity`; `storage.py:import_session` | Config round-trip and old/new-session tests; reject newline/NUL. |
| TODO | `account disable` | Stop labeling future sessions and clear current configured label without rewriting old sessions. | `cli.py:main`; `config.py:update_account_identity` | Old/new-session test. |
| TODO | `account list` | Aggregate current OS user’s history by label, using `Unassigned` for NULL; show sessions/calls/tokens/first/last/cost. | `cli.py:_account`; `storage.py:account_breakdown` | Multi-label golden table. |
| TODO | `account claim-unassigned LABEL` | Explicitly assign all currently unassigned sessions owned by this OS user; reject empty labels; report count. | `cli.py:_account`; `storage.py:claim_unassigned_account` | Two-user database test proving no cross-user update. |
| TODO | `remote list` | List configured SSH aliases, or give the add command when empty. | `cli.py:_remote`; `config.py:load_remote_hosts` | Empty/non-empty snapshot tests. |
| TODO | `remote add HOST` | Validate an OpenSSH config alias, verify connectivity/session directory, store it once, report discovered files, then immediately sync. | `cli.py:_remote`; `config.py:validate_remote_host`; `remote.py:list_remote_rollouts` | Fake-SSH tests for success, duplicate, timeout, missing directory and unsafe alias. |
| TODO | `remote test HOST` | Connect without changing config/data and report Rollout count. | `cli.py:_remote` | Before/after filesystem/database comparison with fake SSH. |
| TODO | `remote sync [HOST] [--force]` | Sync one named host or all configured hosts; incremental by default; print imported/unchanged/failed and inserted turns/calls; return 1 on host/file failure. | `cli.py:_remote`, `_sync_remotes`; `remote.py:sync_remote_rollouts` | Multi-host fake-SSH integration test. |
| TODO | `remote remove HOST` | Remove only configuration; preserve already imported statistics; return 1 if not configured. | `cli.py:_remote` | Config/database before/after test. |
| TODO | `models [--date YYYY-MM-DD]` | Model × reasoning-effort aggregate; omit date for all time. | `cli.py:main`; `tui.py:render_models`; `storage.py:model_breakdown` | Golden sorted table and unknown model/effort test. |
| TODO | `sessions [--limit N]` | Default 20, clamp execution limit to at least one; show recent owned sessions, project/start/turn/call/token/cache/cost. | `cli.py:_sessions`; `storage.py:sessions` | Ordering, ownership, limit and `N/A` tests. |
| TODO | `perf [--date YYYY-MM-DD]` | Show supported OTLP metrics with count/AVG/P50/P95 and TBT-derived TPS; explain bucket approximation; show setup hint when empty. | `cli.py:_perf`; `analytics.py:PERFORMANCE_METRICS`, `performance_summary` | Histogram/gauge/sum differential fixtures. |
| TODO | `cache [--date YYYY-MM-DD]` | Show input/cache write/read, reuse, observed API-equivalent cost, no-cache cost, savings, context amplification, explicit retry tax, OTLP retry attempts/time, and unpriced count. | `cli.py:_cache`; `analytics.py:cache_summary`, `context_and_retry_summary`; `storage.py:telemetry_retry_summary` | Golden numeric fixture including cache writes, retries, and unknown prices. |
| TODO | `projects [--date YYYY-MM-DD]` | Aggregate project sessions/turns/calls/tokens/cache/retry tokens/compactions/cost. | `cli.py:_projects`; `storage.py:project_breakdown` | Multi-project fixture with NULL project and compactions. |
| TODO | `providers [--date YYYY-MM-DD]` | Provider attribution with session/call/token/cache/cost totals and `Unknown` fallback. | `cli.py:_providers`; `storage.py:provider_breakdown` | Golden aggregate test. |
| TODO | `agents [--date YYYY-MM-DD]` | Root/subagent attribution; explicit `agent_role` wins, otherwise parent-thread presence determines role. | `cli.py:_agents`; `storage.py:agent_breakdown` | Root, fork and explicit-role fixture. |
| TODO | `tools [--date YYYY-MM-DD]` | Tool calls, known-outcome success rate, AVG/P50/P95/total duration, ordered by total duration. | `cli.py:_tools`; `storage.py:tool_breakdown`, `tool_durations`; `analytics.py:weighted_percentile` | Known/unknown success and percentile fixture. |
| TODO | `waterfall TURN_ID` | Show one owned turn’s project/status, TTFT/TTFM/E2E, then chronologically merge its LLM and tool events; missing turn returns 1. | `cli.py:_waterfall`; `storage.py:turn_waterfall` | Golden interleaved timeline and cross-user denial test. |
| TODO | `watch [--interval SECONDS] [--iterations N]` | Default 2 seconds; repeatedly refresh local and remote sources and redraw current-day overview; clear an interactive terminal after first iteration; minimum sleep 0.1 seconds; Ctrl-C exits cleanly. | `cli.py:_watch` | Fake clock/import integration test and finite-iteration subprocess test. |
| TODO | `statusline` | Print compact today line: tokens, cache %, calls, cost, TTFT. | `cli.py:_statusline` | Exact one-line golden output including unknown cost/time. |
| TODO | `otel config [--host HOST] [--port PORT]` | Default `127.0.0.1:4318`; print Codex TOML for JSON log/trace/metric exporters and `log_user_prompt = false`. | `cli.py:_otel` | Snapshot and TOML parse test. |
| TODO | `otel serve [--bind ADDR] [--port PORT] [--token TOKEN]` | Start OTLP/HTTP JSON collector; default `127.0.0.1:4318`; optional exact bearer-token check. | `cli.py:_otel`; `collectors/otlp_http.py:OtlpServer` | Real HTTP endpoint suite including auth. |
| TODO | `app-server ingest PATH\|-` | Ingest a server JSONL capture from file or stdin; tolerate malformed lines and report structural/malformed counts. | `cli.py:_app_server`; `collectors/app_server.py:ingest_stream` | File/stdin/malformed subprocess tests. |
| TODO | `app-server proxy [-- SERVER_COMMAND…]` | Transparent bidirectional stdio JSONL proxy; default child `codex app-server --stdio`; preserve child exit code. | `cli.py:_app_server`; `collectors/app_server.py:proxy_stdio` | Fake App Server full-duplex test. |
| TODO | `network probe [HOST] [--port PORT]` | Default `api.openai.com:443`; measure DNS, TCP and TLS setup, TLS version/ALPN; persist result; return 1 on failed probe. | `cli.py:_network`; `network.py:probe_endpoint` | Local TLS endpoint success/failure tests. |
| TODO | `network capture [--host HOST]… [--port PORT] [--interface IFACE] [--duration S] [--packet-limit N]` | Defaults to OpenAI/ChatGPT hosts, port 443, 15 s, 5000 packets; collect only packet direction/length; persist per-destination aggregates; return 1 with safe actionable error. | `cli.py:_network`; `network.py:capture_metadata`, `parse_tcpdump` | Fake tcpdump and parser tests; assert command contains no payload-output flag. |
| TODO | `network show [--limit N]` | Default 30; show recent flow mode/destination/out/in/TTFB/status. | `cli.py:_network`; `storage.py:recent_network` | Ordering and formatting snapshot. |
| TODO | `proxy tunnel [--bind ADDR] [--port PORT]` | Default `127.0.0.1:8899`; HTTP CONNECT only; keep TLS opaque; relay bytes and persist timing/count metadata. | `cli.py:_proxy`; `network.py:TunnelProxyServer` | Real local CONNECT relay test and storage secret scan. |
| TODO | `proxy reverse [--bind ADDR] [--port PORT] [--upstream URL]` | Default `127.0.0.1:8900` and OpenAI upstream; forward GET/POST/DELETE/PATCH/PUT, chunk responses, scan SSE timing, relay WebSocket Upgrade opaquely, honor standard system proxy configuration, and persist metadata only. | `cli.py:_proxy`; `proxy.py:ReverseProxyServer`, `_SseTimingScanner`, `_upstream_connection`, `_open_upstream_socket` | HTTP/SSE/WebSocket/system-proxy integration tests and secret scan. |
| TODO | `proxy tls-init [--directory PATH]` | Generate/reuse a 30-day local RSA CA and localhost certificate with DNS/IP SANs; reject partial existing material; report paths. | `cli.py:_proxy`; `proxy.py:initialize_tls_material` | OpenSSL chain/SAN/mode/reuse/partial-state tests. |
| TODO | `proxy tls [--bind ADDR] [--port PORT] [--upstream URL] [--directory PATH] --acknowledge-sensitive` | Default `127.0.0.1:8901`; refuse with exit 2 without acknowledgement; terminate local TLS v1.2+, re-encrypt upstream, warn which CA is trusted. | `cli.py:_proxy`; `proxy.py:wrap_server_tls` | Refusal snapshot plus real TLS reverse-proxy test. |
| TODO | `export [--from DATE] [--to DATE] [--session ID] [--format json\|jsonl\|csv] [--output PATH]` | Default JSON to stdout; inclusive local-date and owned-session filters; stable per-call metadata fields; JSON keeps Unicode; CSV always emits header; output-file summary goes to stderr. | `cli.py:_export`; `storage.py:export_rows` | Golden files for all formats, empty CSV, filters and stdout/stderr. |
| TODO | `doctor` | Detect Codex/version, Rollouts and recent schema capabilities, configured OTLP exporter without exposing headers, App Server/raw event support, tcpdump, proxies, OpenSSL/TLS state, SQLite integrity, row counts and privacy statement. | `cli.py:_doctor`; `doctor.py:run_doctor` | Fake PATH/config/rollout capability matrix and secret-header redaction test. |
| TODO | `pricing` | List effective catalog rows and USD-per-million rates; state that reasoning is included in output and not double charged. | `cli.py:_pricing`; `pricing.py`; bundled JSON | Snapshot and catalog fixture test. |
| TODO | `demo` | Render deterministic dashboard without accessing or creating Meter home/database; honor `--no-color`. | `cli.py:main`, `_demo` | Before/after filesystem test and golden ANSI/plain snapshots. |

## Rollout JSONL collector

- [ ] **TODO — R-001: Discovery and incremental state.** Recursively discover sorted `rollout-*.jsonl` paths, accept a single file, and treat `(resolved source path, size_bytes, mtime_ns)` as current-import identity. Authority: `session_jsonl.py:discover_rollouts`; `storage.py:file_is_current`, `source_is_current`; `import_files`. Rust verification: filesystem mutation matrix for unchanged, appended, rewritten and forced files.
- [ ] **TODO — R-002: Fail-open parsing.** Decode UTF-8 with replacement, process JSONL one line at a time, count malformed JSON/Unicode lines, ignore unrelated records, but reject a file lacking `session_meta` or a session ID. Authority: `SessionJsonlCollector.collect_file`; `tests/test_collector.py:test_malformed_lines_fail_open`. Rust verification: malformed/mixed/empty fixture suite.
- [ ] **TODO — R-003: Session metadata.** Normalize thread/session ID, start/end timestamps, cwd, project as cwd’s final path component, Git repository/branch, auth mode, Codex version, provider, source path, parent/fork ID, agent role and agent ID. Infer `chatgpt` auth when token-count rate limits include `plan_type`; otherwise retain `unknown`. Authority: `session_jsonl.py:collect_file`; `models.py:SessionRecord`. Rust verification: fixture-to-row assertions for every field.
- [ ] **TODO — R-004: Turn lifecycle/context.** Handle `turn_context`, `task_started`/`turn_started`, `task_complete`/`turn_complete`/`turn_aborted`, and `thread_settings_applied`; preserve model, reasoning effort/mode, service tier, status/error, start/end, E2E and TTFT. Authority: `session_jsonl.py:collect_file`, `_apply_context`, `_apply_settings`, `_error_type`. Rust verification: event-sequence permutations and incomplete-turn tests.
- [ ] **TODO — R-005: Token key compatibility.** Accept snake_case and camelCase input/cached/cache-write/output/reasoning/total keys, clamp negatives to zero, and compute cache miss as `max(input - cached - write, 0)`. Authority: `models.py:TokenUsage`. Rust verification: table-driven key/negative/missing-value tests.
- [ ] **TODO — R-006: Cumulative usage deltas.** For `token_count`, derive per-call usage from changes in `total_token_usage`; on accumulator reset use `last_token_usage`; suppress zero/duplicate events and count them. Preserve context model/provider/effort/mode/tier. Authority: `session_jsonl.py:collect_file`, `_usage_identity`; `tests/test_collector.py`. Rust verification: exact Python fixtures for cumulative, duplicate and reset cases.
- [ ] **TODO — R-007: Exact raw-response preference.** Accept both `raw_response_completed` and `rawResponse/completed`; create exact per-response usage; suppress the matching later cumulative delta so tokens are not double-counted. Authority: `session_jsonl.py:collect_file`; `test_raw_response_usage_wins_over_matching_cumulative_delta`. Rust verification: paired-event fixture and database count assertion.
- [ ] **TODO — R-008: Stable deduplication.** Generate SHA-256 semantic fingerprints so forked/replayed cumulative usage with rewritten timestamps remains one call; use response ID for exact calls; database uniqueness must keep re-import idempotent. Authority: `session_jsonl.py:_make_call`, `_usage_identity`; `storage.py:import_session`; collector idempotency/fork tests. Rust verification: import parent/child/re-import fixtures and compare fingerprints/counts.
- [ ] **TODO — R-009: Fork ownership.** When a copied fork turn already belongs to an authoritative session, newly observed calls for that turn remain attached to the original turn/session while both sessions may exist. Authority: `storage.py:import_session` call-session resolution. Rust verification: fork-prefix database fixture.
- [ ] **TODO — R-010: Tool events.** Pair begin/end events for exec/shell, patch/apply_patch, MCP and web search; name MCP tools with server/tool components; normalize start/end/duration, exit code and success; store only completed pairs. Authority: `session_jsonl.py:_TOOL_BEGIN`, `_TOOL_END`, `_tool_name`, `_tool_success`, `_duration_ms`. Rust verification: all tool types, missing begin/end, duration encodings and outcome variants.
- [ ] **TODO — R-011: Capability reporting.** Track session JSONL, token usage, exact per-response usage, cache-write, turn timing, compaction and tool-call capabilities as discovered. Authority: `SessionJsonlCollector.capabilities`, `ParsedSession.capability_names`, `doctor.py:_latest_rollout_capabilities`. Rust verification: capability fixture matrix.
- [ ] **TODO — R-012: Streaming parser.** Parse local files and remote in-memory streams with the same normalization, without requiring a temporary raw file. Authority: `session_jsonl.py:_StreamPath`, `collect_stream`; `remote.py:_import_batch`. Rust verification: identical parsed records for file and reader inputs plus filesystem secret scan.

## Pricing and cost semantics

- [ ] **TODO — C-001: Catalog override and snapshots.** Load `<meter-home>/pricing.json` when present, otherwise bundled `codex_meter/data/pricing.json`; parse versioned entries and insert unique snapshots keyed by model/provider/effective/version. Authority: `cli.py:main`; `pricing.py:PricingCatalog.bundled`; migration 001. Rust verification: override/bundled and idempotent snapshot tests.
- [ ] **TODO — C-002: Historical resolution.** Resolve exact model/provider to the latest entry whose `effective_from <= call timestamp`; default provider to `openai`; alias `gpt-5.6` to `gpt-5.6-sol`; unknown/unpriced models return no price. Authority: `pricing.py:resolve`. Rust verification: before/between/after date, alias, provider and unknown cases.
- [ ] **TODO — C-003: Separate token rates.** Bill regular input only on `input - cached_input - cache_write`, then separately bill cached reads, cache writes and output. Reasoning tokens are a subset of output and must never be added again. Authority: `pricing.py:calculate`; `tests/test_pricing.py`. Rust verification: exact cost-component fixtures.
- [ ] **TODO — C-004: Long-context pricing.** When input exceeds (strictly `>`) `long_context_threshold`, apply the configured input multiplier to all input categories and output multiplier to output for the whole call. Authority: `pricing.py:calculate`. Rust verification: threshold−1, threshold, threshold+1 tests.
- [ ] **TODO — C-005: API-equivalent wording and unknowns.** UI/report costs are estimates labeled `$… eq`; unknown price is `N/A`; aggregate cost excludes unpriced calls and surfaces their count/warning rather than treating them as zero-priced. Authority: `tui.py:_money`, `render_overview`; CLI report formatters. Rust verification: mixed priced/unpriced golden reports.
- [ ] **TODO — C-006: Savings calculation.** `without_cache` prices the entire input at regular input rate plus output; savings is `without_cache - observed`; preserve catalog pricing version per call. Authority: `pricing.py:CostBreakdown`, `calculate`; `analytics.py:cache_summary`. Rust verification: differential calculation tests.

## Account weekly quota

- [ ] **TODO — Q-001: Source and credentials boundary.** Read quota by spawning `codex app-server --stdio` and sending JSON-RPC `initialize`, `initialized`, then `account/rateLimits/read`; never read or store Codex auth credentials. Authority: `quota.py:read_weekly_quotas`. Rust verification: fake stdio server checks request sequence and credential-canary remains unread/unpersisted.
- [ ] **TODO — Q-002: Robust transport.** Default timeout is 8 seconds; tolerate unrelated/malformed lines until response ID 2; surface spawn, protocol, rejection and timeout failures as actionable unavailable messages; terminate/kill and reap child reliably. Authority: `quota.py:read_weekly_quotas`. Rust verification: fake server failure matrix and process-leak check.
- [ ] **TODO — Q-003: Bucket extraction.** Read both `rateLimitsByLimitId` and legacy `rateLimits`, deduplicate limit IDs, inspect primary/secondary windows, accept seven-day windows within ±12 hours of 10,080 minutes, choose closest, clamp used percent to 0–100, preserve reset/plan/name, and order main `codex` first. Authority: `quota.py:extract_weekly_quotas`. Rust verification: port all `tests/test_quota.py` fixtures.
- [ ] **TODO — Q-004: Presentation.** At the very top of overview content show each bucket’s name, remaining %, local reset date/time and a used-percentage bar, including 0% and 100%; use warning color at ≤25% remaining. Explain that this live account-wide quota is independent from local Week/project filters. Authority: `tui.py:render_overview`, `_weekly_quota_lines`; README/user guide. Rust verification: 0/18/100/unavailable golden UI snapshots in narrow and wide terminals.
- [ ] **TODO — Q-005: Non-blocking dashboard refresh.** First dashboard paint shows “Loading…” while quota loads on a daemon/background task; completion triggers redraw without a key; `r` retries; errors include the concrete reason. Authority: `cli.py:_interactive_dashboard`; `tests/test_tui.py:test_interactive_dashboard_does_not_wait_for_live_quota`. Rust verification: PTY test with delayed/failing fake App Server.

## SSH remote Rollout aggregation

- [ ] **TODO — S-001: Configuration and alias safety.** `[remotes].hosts` is an ordered, de-duplicated list. Accept only 1–128-character aliases matching letters/numbers followed by letters/numbers/dot/underscore/hyphen; reject whitespace, options and shell syntax. Preserve unrelated TOML sections when updating. Authority: `config.py:validate_remote_host`, `load_remote_hosts`, `update_remote_hosts`. Rust verification: round-trip and injection corpus tests from `tests/test_remote.py`.
- [ ] **TODO — S-002: SSH invocation.** Use the user’s OpenSSH config and non-interactive options equivalent to `BatchMode=yes`, 8-second connect timeout, 10-second keepalive and three missed keepalives; never bypass host-key verification. Authority: `remote.py:SSH_COMMAND`. Rust verification: inspect fake-SSH argv and an integration alias.
- [ ] **TODO — S-003: Portable discovery.** On the remote POSIX shell, require `$HOME/.codex/sessions`, recursively find `rollout-*.jsonl`, obtain size and mtime using GNU or BSD `stat`, and return only normalized metadata before transfer. Authority: `remote.py:_LIST_SCRIPT`, `list_remote_rollouts`. Rust verification: Linux and macOS shell fixture plus missing-directory case.
- [ ] **TODO — S-004: Safe paths.** Permit only relative, non-traversing, line-safe paths whose basename starts `rollout-` and suffix is `.jsonl`; normalize a leading `./`; construct source IDs as `ssh://HOST/~/.codex/sessions/RELATIVE`. Authority: `remote.py:_safe_relative_path`, `RemoteFile.source_path`. Rust verification: traversal/control-character/absolute-path corpus.
- [ ] **TODO — S-005: Incremental streaming.** Compare remote size/mtime against `import_files`, batch changed paths in groups of 64, stream a remote tar archive over SSH, parse members in memory, and never save raw remote Rollouts or temporary copies. Authority: `remote.py:sync_remote_rollouts`, `_import_batch`. Rust verification: fake tar/SSH integration, unchanged/force cases, large streaming fixture and secret filesystem scan.
- [ ] **TODO — S-006: Failure accounting.** Track discovered/imported/skipped/failed files and inserted turns/calls/tools; detect missing/unrequested/unsafe tar entries and non-zero/timeout/invalid archive failures; continue other configured hosts. Authority: `remote.py:RemoteSyncResult`; `cli.py:_sync_remotes`. Rust verification: partial-archive and multi-host failure tests.
- [ ] **TODO — S-007: Aggregation semantics.** Remote sessions are attributed to the local Meter OS user/account label at import and merge into day/week/month/all-time, history, project, model and response-performance views; event/thread dedupe still applies across local and remote copies. Authority: `storage.py:import_session`, owned query helpers; `CHANGELOG.md:0.15.0`. Rust verification: same Rollout locally/remotely plus unique remote fixture; assert no double counting and correct filters.
- [ ] **TODO — S-008: Interactive behavior.** Configured remotes appear as `LOCAL + N REMOTE`; initial and `r` sync run in background with a separate SQLite connection, show syncing/success/actionable failure status, and trigger project-list/content redraw without delaying launch. Authority: `cli.py:_interactive_dashboard`. Rust verification: delayed fake SSH PTY test and newly discovered project test.

## OTLP/HTTP JSON collector and performance metrics

- [ ] **TODO — O-001: HTTP endpoints.** `GET /healthz` and `/readyz` return `200 {"status":"ok"}`; POST accepts `/v1/logs`, `/v1/metrics`, `/v1/traces`; unknown paths return 404. Authority: `collectors/otlp_http.py:OtlpRequestHandler`. Rust verification: real HTTP route tests.
- [ ] **TODO — O-002: Input controls.** Accept empty or `application/json` content type, optional gzip body, reject invalid length/JSON/root, enforce 8 MiB compressed request limit, require exact `Authorization: Bearer TOKEN` when configured, and emit no HTTP access log. Authority: `otlp_http.py:MAX_BODY_BYTES`, `do_POST`, `log_message`. Rust verification: status-code matrix, gzip/size/auth tests, captured stderr assertion.
- [ ] **TODO — O-003: Metric shapes.** Parse OTLP gauge, sum, histogram, exponentialHistogram and summary data points, including scalar/sum/count/min/max/bounds/buckets, nanosecond timestamps and correlation attributes. Authority: `otlp_http.py:parse_metrics`; `models.py:MetricPointRecord`. Rust verification: one fixture per metric kind and database comparison.
- [ ] **TODO — O-004: Logs and traces.** Parse resource/scope logs; derive safe event names; convert spans into `span:<safe-name>` log records and calculate duration; use `otel.log`/`otel.span` fallbacks. Authority: `otlp_http.py:parse_logs`, `parse_traces`, `_safe_body_event_name`, `_safe_span_name`. Rust verification: valid/unsafe name and timestamp fixtures.
- [ ] **TODO — O-005: Attribute allowlist.** Persist only the exact safe key set in `SAFE_ATTRIBUTE_KEYS`, stringify scalar OTLP values and truncate each value to 256 characters. Never stringify/export OTLP config headers in doctor output. Authority: `otlp_http.py:SAFE_ATTRIBUTE_KEYS`, `_safe_attributes`; `doctor.py:_otel_exporter_name`. Rust verification: secret arbitrary attributes and authorization-header canaries absent from all files.
- [ ] **TODO — O-006: Metric correlation.** When IDs exist, enrich turns with E2E/TTFT/TTFM and token types, and calls with request duration, overhead, inference, TTFB/TTFT and average TBT; use `COALESCE`/`MAX` semantics from Python. Authority: `storage.py:_apply_metric_point`. Rust verification: insert-before/after correlated-row tests.
- [ ] **TODO — O-007: Performance aggregation.** Collapse cumulative series to the latest point per `(name, attributes, start_time-or-fingerprint)`; weighted-average scalar samples; approximate P50/P95 from histogram bucket representatives; derive TPS only for positive `_tbt.duration_ms`. Authority: `analytics.py:performance_summary`, `weighted_percentile`. Rust verification: Python/Rust differential fixtures including cumulative snapshots.

## App Server adapter

- [ ] **TODO — A-001: JSON-RPC correlation.** Track safe client request context for `thread/start`, `thread/resume`, `thread/fork`, and `turn/start`, then correlate responses by request ID. Authority: `collectors/app_server.py:AppServerAdapter.ingest`, `_handle_response`, `_safe_request_context`. Rust verification: request/response order and missing-response tests.
- [ ] **TODO — A-002: Lifecycle.** Normalize `thread/started`, `turn/started`, `turn/completed`, `item/started`, `item/completed`, `thread/compacted`/`compacted`, `model/rerouted`, and `item/agentMessage/delta`. Preserve turn status and live timestamps. Authority: `app_server.py:AppServerAdapter`. Rust verification: port `test_app_server_adapter_extracts_usage_tools_and_compaction_without_content` plus per-event fixtures.
- [ ] **TODO — A-003: Exact usage and speeds.** Normalize `rawResponse/completed` into exact per-response usage/cost; record first event/message, request duration, TTFT, TTFM, generation duration and output TPS when derivable. Authority: `app_server.py:_raw_response`; `storage.py:insert_live_call`. Rust verification: deterministic timestamp fixture and exact columns assertion.
- [ ] **TODO — A-004: Thread token totals.** Handle `thread/tokenUsage/updated` as per-thread cumulative totals and apply only positive deltas to the turn using MAX semantics. Authority: `app_server.py:_token_usage`; `storage.py:update_live_turn_usage`. Rust verification: cumulative/reset/duplicate fixture.
- [ ] **TODO — A-005: Tool mapping.** Map command execution, file changes, MCP, collaboration, web search, image view and sleep; retain safe tool IDs/names, time, success, exit and compactions without payloads. Authority: `app_server.py:_item`, `_tool_name`, `_compaction`. Rust verification: all item-type fixtures and secret scan.
- [ ] **TODO — A-006: Transparent proxy augmentation.** Relay every original line even if malformed; inspect only in memory; on client `thread/start`, add `experimentalRawEvents=true` only when absent to enable exact response usage. Authority: `app_server.py:_pump`; proxy test. Rust verification: byte-level relay cases for unchanged/malformed/augmented lines and no-content persistence.
- [ ] **TODO — A-007: Identifier/path sanitization.** Limit identifiers to 256 safe characters, paths to 4096 without NUL, and use emitted timestamps when valid or current UTC otherwise. Authority: `app_server.py:_safe_identifier`, `_safe_path`, `_event_time`. Rust verification: fuzz/property tests for unsafe metadata.

## Network diagnostics and proxies

- [ ] **TODO — N-001: Response-performance view.** For owned turns show local time, model, output, TTFT, E2E and exact call TPS when available; otherwise estimate `output_tokens / (E2E - TTFT)` only for valid positive intervals and mark estimates `*`. Show AVG/P50/P95 and sample counts. Authority: `storage.py:response_performance_range`; `tui.py:render_network`, `_output_rate`. Rust verification: exact/estimated/unavailable golden view.
- [ ] **TODO — N-002: Socket probe.** Measure DNS resolution, first-address TCP connect, verified TLS handshake, TLS version, ALPN (`h2`, `http/1.1`), overall duration and safe error type. Authority: `network.py:probe_endpoint`. Rust verification: controlled DNS/local TLS success and refused-connection cases.
- [ ] **TODO — N-003: Passive capture privacy.** Invoke tcpdump with `-nn -tt -l -q` and count limit/BPF filter, never `-X`, `-A` or `-w`; parse IPv4/IPv6 direction and packet length only; aggregate counts/bytes/duration by remote IP. Authority: `network.py:capture_metadata`, `parse_tcpdump`. Rust verification: argv assertion, parser fixtures and storage schema inspection.
- [ ] **TODO — N-004: Capture interface portability.** Parse `tcpdump -D` and prefer `any`, then `pktap`, then `en0`, otherwise first available; return actionable not-found/no-interface/no-host/permission errors. Authority: `network.py:_default_capture_interface`, `_safe_tcpdump_error`. Rust verification: Linux/macOS/empty listings and permission stderr fixtures.
- [ ] **TODO — N-005: CONNECT proxy.** Accept only CONNECT, default missing port to 443, support bracketed IPv6, cap headers at 64 KiB, measure DNS/TCP and bidirectional bytes, keep TLS opaque, and store a flow even on failure. Authority: `network.py:TunnelProxyHandler`, `_parse_connect_target`, `_relay`. Rust verification: HTTP method/IPv4/IPv6/header-limit/relay tests.
- [ ] **TODO — N-006: Reverse HTTP forwarding.** Validate HTTP(S) upstream; remove hop-by-hop headers, cap request bodies at 64 MiB, preserve upstream status/non-hop headers, stream downstream with chunked encoding, record sizes/timing/status, and never persist content/headers. Authority: `proxy.py:ReverseProxyServer`, `ReverseProxyHandler._forward`. Rust verification: all supported methods, oversized request, chunked response and secret scan.
- [ ] **TODO — N-007: SSE timing.** Scan at most the trailing 64 KiB of in-memory SSE line data; record first event and first output for the recognized response events without storing event data. Authority: `proxy.py:_SseTimingScanner`. Rust verification: fragmented/multiple/oversized SSE fixture.
- [ ] **TODO — N-008: WebSocket relay.** Forward Upgrade handshake and opaque frames, preserve relevant headers but not proxy authorization, require upstream 101 for success, and store only byte counts/status/duration. Authority: `proxy.py:_forward_websocket`, `_relay_sockets`. Rust verification: local echo WebSocket integration and payload secret scan.
- [ ] **TODO — N-009: Environment proxy support.** Honor standard system HTTP/HTTPS proxy discovery and bypass rules for HTTP and WebSocket upstreams; support Basic proxy credentials in transit without persistence. Authority: `proxy.py:_upstream_connection`, `_open_upstream_socket`. Rust verification: authenticated local proxy and bypass tests with database/filesystem secret scan.
- [ ] **TODO — N-010: TLS diagnostics safety.** Require explicit acknowledgement, generate rather than download CA material, use TLS 1.2 minimum locally, present a complete leaf+CA chain, re-encrypt and verify upstream, never overwrite a partial key set, and make trust-removal warnings visible. Authority: `cli.py:_proxy`; `proxy.py:initialize_tls_material`, `wrap_server_tls`. Rust verification: certificate and live proxy suite on supported platforms.
- [ ] **TODO — N-011: Network flow identity/storage.** Preserve all timing/count/status/correlation/quality fields and semantic SHA-256 fingerprints so repeated insertions are idempotent. Authority: `models.py:NetworkFlowRecord`; `storage.py:insert_network_flow`; fingerprint helpers. Rust verification: row-level differential fixtures.

## SQLite schema and persistence

- [ ] **TODO — DB-001: Connection behavior.** Enable foreign keys, WAL journal mode and `synchronous=NORMAL`; permit independent background connections; serialize writes/transactions safely. Authority: `storage.py:Storage.__init__`, `transaction`. Rust verification: PRAGMA assertions and concurrent local/remote/quota/UI workload test.
- [ ] **TODO — DB-002: Migration ledger.** Preserve ordered SQL migrations and `schema_migrations(version PRIMARY KEY, applied_at)` behavior; migrations are idempotent and can upgrade a v0.15.0 Python database in place. Authority: `storage.py:migrate`; `migrations/*.sql`. Rust verification: empty, migration-001, migration-002 and current database upgrade tests.
- [ ] **TODO — DB-003: `sessions`.** Preserve columns: `id`, unique `codex_thread_id`, start/end, cwd/project, Git repo/branch, auth mode/version/provider, source/source_path, parent thread/agent role/agent ID, created/updated, owner UID/username and optional account label. Preserve indexes on start, project, owner and account. Authority: migrations 001/003. Rust verification: `PRAGMA table_info/index_list` golden comparison and data round-trip.
- [ ] **TODO — DB-004: `turns`.** Preserve unique Codex turn ID, session FK cascade, lifecycle/context, six token counters, cost, TTFT/TTFM/E2E/tool/model times, error and quality fields; retain session/completed/model indexes. Authority: migration 001. Rust verification: schema comparison and upsert/aggregate tests.
- [ ] **TODO — DB-005: `llm_calls`.** Preserve unique event fingerprint and response ID, session/turn FKs, all timestamps, requested/actual model/provider/reasoning/transport/tier, six token counters, request/network/generation/inference/overhead/TBT/TPS metrics, retry/success/error, cost/version and quality fields; preserve indexes. Authority: migration 001. Rust verification: schema comparison and full-row import/live round-trip.
- [ ] **TODO — DB-006: `tool_calls`.** Preserve unique source call ID, session/turn/call relationships, tool/times/duration/outcome/exit and quality fields; turn index and FK actions must match. Authority: migration 001. Rust verification: schema comparison and upsert behavior.
- [ ] **TODO — DB-007: `pricing_snapshots` and `import_files`.** Preserve pricing components/version uniqueness and source metadata (path/size/mtime/session/import time/malformed/duplicate counts). Authority: migration 001; `storage.py:sync_pricing`, `import_session`. Rust verification: idempotency and changed-source tests.
- [ ] **TODO — DB-008: `metric_points`.** Preserve unique fingerprint, scalar/distribution/timestamps/JSON attributes/correlation and quality columns plus name/time and turn indexes. Authority: migration 002; `storage.py:insert_metric_points`. Rust verification: schema and all OTLP kind round-trips.
- [ ] **TODO — DB-009: `telemetry_logs`.** Preserve unique fingerprint, safe event metadata/JSON attributes/correlation/outcomes/source and event/time index. Authority: migration 002; `storage.py:insert_telemetry_logs`. Rust verification: schema and duplicate tests.
- [ ] **TODO — DB-010: `compactions`.** Preserve unique fingerprint, thread/turn/time/source/confidence and thread/time index. Authority: migration 002; `storage.py:insert_compaction`. Rust verification: schema, duplicate and project aggregate tests.
- [ ] **TODO — DB-011: `network_flows`.** Preserve unique fingerprint, endpoint/protocol/TLS/HTTP, byte/packet/timing/outcome/correlation/quality fields and time/destination indexes. Authority: migration 002; `storage.py:insert_network_flow`. Rust verification: schema and every diagnostic-mode round-trip.
- [ ] **TODO — DB-012: Session upsert semantics.** On existing thread, update end/source path and prefer non-null/new known metadata without replacing an existing account label; backfill missing owner fields during migration. Authority: `storage.py:import_session`, `migrate`. Rust verification: sparse/richer/reimport and identity-change tests.
- [ ] **TODO — DB-013: Turn/call/tool upserts.** Turn updates use the Python COALESCE/status rules; calls are insert-or-ignore by semantic fingerprint/response ID; tools insert-or-ignore for Rollouts and live tools update completion fields. Recompute turn token/cost/tool totals after related inserts. Authority: `storage.py:import_session`, `upsert_live_turn`, `insert_live_call`, `upsert_live_tool`, `_refresh_turn_aggregates`. Rust verification: ordered/reordered/duplicate event tests.
- [ ] **TODO — DB-014: Integrity/count inspection.** Preserve `PRAGMA integrity_check` and row counts for sessions, turns, calls, tools, pricing, metric points, telemetry logs, compactions and network flows. Authority: `storage.py:integrity_check`, `counts`; doctor output. Rust verification: known fixture count snapshot.

## Query and reporting semantics

- [ ] **TODO — DQ-001: OS-user ownership is mandatory.** Every user-history/call/tool/project/waterfall query uses owner UID when available, otherwise owner username. A shared database must never expose another OS user’s usage in those views. Authority: `storage.py:_owned_call_filter` and query-specific owner clauses. Rust verification: two-user shared-database suite for every command/view/export.
- [ ] **TODO — DQ-002: Optional account/project composition.** Account and project filters compose with ownership and date; `Unassigned` maps to SQL NULL; project NULL displays/filters as `Unknown`; default is all accounts/projects. Authority: `storage.py:_owned_call_filter`. Rust verification: Cartesian filter matrix.
- [ ] **TODO — DQ-003: Local-time dates.** Filtering and grouping use SQLite-equivalent local calendar semantics: inclusive from/to dates, day, Monday-start week, and first-of-month buckets. Authority: `storage.py:_owned_call_filter`, `usage_history`; `cli.py:_period_bounds`. Rust verification: fixed-timezone DST and boundary fixtures compared to Python.
- [ ] **TODO — DQ-004: Overview.** Count calls/distinct sessions/distinct turns; sum six token/cost fields; count unpriced calls; average TTFT/E2E only across distinct selected turns. Authority: `storage.py:overview_range`. Rust verification: calls sharing turns and mixed null-price fixture.
- [ ] **TODO — DQ-005: Model breakdown.** Group by nullable model/effort with `Unknown` display, sum token/cost values and unpriced calls, order by total tokens descending. Authority: `storage.py:model_breakdown_range`. Rust verification: ordering/tie/null fixture.
- [ ] **TODO — DQ-006: History.** Group calls by local day/Monday/week/month; count distinct sessions/turns and sum input/cached/output/total/cost; ascending bucket order. Authority: `storage.py:usage_history`. Rust verification: differential SQL golden dataset.
- [ ] **TODO — DQ-007: Project ordering.** Interactive project names are de-duplicated and ordered by most recent call/session activity descending, then case-insensitively by name; `Unknown` represents NULL. Authority: `storage.py:project_names`; `interactive.py:_sync_projects`. Rust verification: activity/tie/NULL/duplicate fixture.
- [ ] **TODO — DQ-008: Project aggregate.** Compactions are joined by thread/project; retry tokens include calls with `retry_index > 0`; order by total tokens descending. Authority: `storage.py:project_breakdown`. Rust verification: multi-thread/project fixture.
- [ ] **TODO — DQ-009: Provider and agent aggregates.** Provider fallback order is call, session, `Unknown`; agent fallback is explicit role, otherwise root when no parent and subagent when parent exists. Authority: `storage.py:provider_breakdown`, `agent_breakdown`. Rust verification: null/fallback fixtures.
- [ ] **TODO — DQ-010: Tool analytics.** Success percentage denominator includes only known outcomes; AVG/MAX/total derive from non-null SQL durations and P50/P95 use nearest cumulative weighted rank. Authority: `storage.py:tool_breakdown`, `tool_durations`; `analytics.py:weighted_percentile`. Rust verification: even/odd/null distributions.
- [ ] **TODO — DQ-011: Cache/retry analytics.** Reuse is cached/input; context amplification is last positive input divided by first for turns with multiple calls; explicit retries are `retry_index > 0`; OTLP retry count/time reads `codex.api_request` logs whose `attempt > 0`. Authority: `analytics.py`; `storage.py:telemetry_retry_summary`. Rust verification: differential analytics fixture.
- [ ] **TODO — DQ-012: Export ordering/fields.** Sort calls by completion time and emit exactly: `session_id`, `turn_id`, `response_id`, `completed_at`, `model`, `reasoning_effort`, six token fields, `cost_usd`, `data_source`, `confidence`, `estimated`. Authority: `storage.py:export_rows`; `cli.py:_export`. Rust verification: key/header/order snapshots.
- [ ] **TODO — DQ-013: Network scope.** Project-filtered recent flows correlate `network_flows.thread_id` to owned sessions; unfiltered recent flows retain Python’s current global-flow behavior. Response timing is always current-owner scoped. Authority: `storage.py:recent_network`, `response_performance_range`. Rust verification: explicit compatibility fixture, including uncorrelated probe flows.

## Interactive dashboard and rendering

- [ ] **TODO — UI-001: Views/menu order.** Bottom menu order is Today, Week, Month, All time, Daily history, Weekly history, Monthly history, Network, Project, Refresh, Help, Quit. Initial active view is Today and default scope is All projects. Authority: `interactive.py:MENU_ITEMS`, `InteractiveState`. Rust verification: state and screen snapshots.
- [ ] **TODO — UI-002: Main navigation.** Arrow keys wrap selection; Enter/Space opens; `/` opens command input; `r` refreshes; `?` opens help; `q` and Ctrl-C quit only in normal mode; Esc closes help and restores cursor to active view. Authority: `interactive.py:handle_key`. Rust verification: state-machine table tests.
- [ ] **TODO — UI-003: Slash palette.** Accept typed text up to 48 characters, spaces/backspace, case-insensitive normalized matching, arrows with wrap, five suggestions per page, Enter selected/exact command, Esc back, and English one-line descriptions. `q` is text while palette is open; Ctrl-C still quits. Authority: `interactive.py:COMMAND_ITEMS`, `_handle_command_key`, `_command_suggestions`, `_command_palette_lines`. Rust verification: port slash tests and golden narrow/wide pages.
- [ ] **TODO — UI-004: Slash commands/aliases.** Support `/today`/`day`, `/week`, `/month`, `/all`, `/history day|week|month`, `/daily|weekly|monthly`, `/network`, `/project|projects`, `/refresh|reload`, `/help|?`, and `/quit|exit|q`; unknown command reports `/help` guidance. Authority: `interactive.py:parse_slash_command`. Rust verification: table-driven alias tests.
- [ ] **TODO — UI-005: Project picker.** Preserve recent ordering; prepend All projects when no search; type Unicode/space query up to 48 characters; case-fold substring filter; arrows wrap; Enter applies; Space applies only with empty query; Backspace edits/restores current selection; Esc cancels; `q` is filter text; page size adapts up to eight rows. Authority: `interactive.py:_handle_project_key`, `_project_picker_text`, `_project_choices`. Rust verification: port all project-picker tests including CJK and spaces.
- [ ] **TODO — UI-006: Refresh behavior.** `r` closes help, imports changed local Rollouts immediately, triggers background quota and remote refresh, updates project choices, shows progress/success/actionable error, and does not quit or corrupt the terminal. Authority: `interactive.py:run_interactive`; `cli.py:_interactive_dashboard.refresh`. Rust verification: PTY failure/success workflow.
- [ ] **TODO — UI-007: Terminal lifecycle.** Require TTY, enter alternate screen, hide cursor, restore cursor/screen and terminal settings on normal exit, error, Ctrl-C and panic-equivalent paths. POSIX uses cbreak input; Windows uses native console input and VT output. Authority: `interactive.py:run_interactive`. Rust verification: PTY tests plus Windows console smoke test.
- [ ] **TODO — UI-008: Key decoding.** Decode POSIX CSI arrows without consuming a following Enter; support UTF-8 input; map Windows extended arrows, Enter, Space, Backspace, Esc and Ctrl-C. Authority: `interactive.py:_read_key`, `_read_windows_key`, `_decode_windows_character`. Rust verification: byte-stream/unit tests on each platform.
- [ ] **TODO — UI-009: Responsive layout.** Clamp dashboard width to 40–132 columns (Network minimum 80); use Unicode display-column width for CJK/combining text; at width <80 show compact one-item menu; wrap wide menus; short terminals retain the top quota/summary and closing frame with an enlargement warning. Authority: `tui.py`, `interactive.py:render_interactive_screen`, `_clip_body`, width helpers. Rust verification: golden snapshots at 40×12, 80×12, 100×30 and 180×18 with width assertions.
- [ ] **TODO — UI-010: Color behavior.** Preserve dark blue/cyan/light/muted/green/yellow ANSI palette; disable color with `--no-color`, redirected report output, or `NO_COLOR`; emit no escapes in plain mode. Authority: `tui.py` constants and `_supports_color`; CLI render calls. Rust verification: ANSI/plain snapshots and environment matrix.
- [ ] **TODO — UI-011: Overview content.** Render title/source/period; weekly quota first; remote status; tokens, API-equivalent cost, cache %, calls; input/output/reasoning; cache read/miss/write; sessions/turns/AVG TTFT/E2E; unknown-pricing warning; three denominator-explained bars; up to eight model/effort rows; empty-state guidance. Authority: `tui.py:render_overview`. Rust verification: populated/empty/unpriced/quota/remote golden snapshots.
- [ ] **TODO — UI-012: Percentage meanings.** Cache is cached input divided by total input; Reasoning is reasoning output divided by output; quota Used is backend seven-day used percentage. Do not render the Input bar as a misleading 100% percentage. Authority: `tui.py:render_overview`; `tests/test_tui.py:test_token_ratio_labels_explain_their_denominators`. Rust verification: exact label/denominator assertions.
- [ ] **TODO — UI-013: History and Network screens.** Preserve compact history columns/empty state and Network timing/speed/connection panels, estimate marker, explanatory footnote and project/user labels. Authority: `tui.py:render_history`, `render_network`. Rust verification: golden views with unknown cost/speed/no flow/failure flow.
- [ ] **TODO — UI-014: Background redraw.** Quota and remote workers signal UI updates; polling must redraw and resynchronize projects without requiring input while keeping keyboard responsive. Authority: `cli.py:_interactive_dashboard.consume_dashboard_update`; `interactive.py:run_interactive`. Rust verification: delayed worker PTY test with no keystrokes.

## Configuration compatibility

- [ ] **TODO — CFG-001: Default TOML.** Preserve sections and values: `[privacy] store_prompt=false, store_response=false, store_tool_output=false, store_headers=false, diagnostic_payload_logging=false`; `[retention] raw_days=7, call_days=90`; `[collector] batch_size=500, fail_open=true`; `[network] store_payloads=false, store_headers=false, passive_capture=false, tls_diagnostic=false`; `[identity] account_tracking=false, account_label=""`; `[remotes] hosts=[]`. Authority: `config.py:DEFAULT_CONFIG`. Rust verification: exact TOML parse/value snapshot.
- [ ] **TODO — CFG-002: Current effective versus reserved keys.** Preserve all keys even though v0.15.0 runtime currently reads only identity/remotes; do not silently make reserved retention/network/privacy toggles weaken privacy. Authority: `config.py:load_identity`, `load_remote_hosts`; collector privacy invariants. Rust verification: config matrix proves reserved values do not enable payload persistence.
- [ ] **TODO — CFG-003: Malformed optional config.** A malformed/hand-edited optional config must not prevent core collection; identity/remotes safely fall back to disabled/empty. Authority: `config.py:load_identity`, `load_remote_hosts`. Rust verification: malformed TOML startup/import test.
- [ ] **TODO — CFG-004: Targeted edits.** Account and remote commands rewrite only their own TOML section, retain all other text/sections, use valid Unicode string escaping, de-duplicate remotes, and preserve order. Authority: `config.py:update_account_identity`, `update_remote_hosts`. Rust verification: comments/custom-section preservation round trips.
- [ ] **TODO — CFG-005: Runtime environment.** Honor `CODEX_METER_HOME`, `CODEX_HOME`, `NO_COLOR`, standard HTTP(S)/no-proxy variables, and diagnostic-only `CODEX_METER_DEBUG_PROXY=1` safe path/status logging; do not expose query strings in debug logs. Authority: `cli.py`, `tui.py`, `proxy.py`. Rust verification: isolated environment tests with secret query values.

## Doctor and capability diagnostics

- [ ] **TODO — DR-001: Codex detection.** Resolve `codex` on PATH, run `codex --version` with timeout, and report yes/no plus safe version detail. Authority: `doctor.py:run_doctor`, `_command`. Rust verification: fake executable success/failure/timeout.
- [ ] **TODO — DR-002: Rollout capability detection.** Count local Rollouts and scan at most the newest ten for reasoning usage, cached input, cache write and TTFT keys, reporting yes or unknown. Authority: `doctor.py:_latest_rollout_capabilities`. Rust verification: ordered capability fixtures.
- [ ] **TODO — DR-003: OTLP diagnosis/redaction.** Parse `$CODEX_HOME/config.toml`; report none/string/otlp-http/otlp-grpc/configured without serializing exporter objects or headers. Authority: `doctor.py:_read_toml`, `_otel_exporter_name`. Rust verification: secret authorization header absent from stdout/stderr/files.
- [ ] **TODO — DR-004: App Server schema probe.** Detect `codex app-server --help`; when available generate experimental JSON schema in a temporary directory and identify `v2/RawResponseCompletedNotification.json` by title; clean temporary files. Authority: `doctor.py:_app_server_has_raw_response`. Rust verification: fake schema generator and cleanup test.
- [ ] **TODO — DR-005: System capabilities.** Report built-in OTLP logs+metrics+traces, tcpdump path/absence, CONNECT/reverse availability, OpenSSL-dependent explicit TLS state, SQLite integrity and database path/counts. Authority: `doctor.py:run_doctor`; `cli.py:_doctor`. Rust verification: PATH capability matrix and corrupted-database case.

## Cross-platform and release behavior

- [ ] **TODO — X-001: Supported targets.** Build and smoke-test Linux x86_64, macOS arm64, macOS x86_64 and Windows x86_64 standalone binaries. Authority: `.github/workflows/release.yml`; v0.15.0 artifacts. Rust verification: CI matrix runs `--version`, `--no-color demo`, doctor, OTLP config, network show, remote list, App Server stdin ingest and TLS acknowledgement refusal.
- [ ] **TODO — X-002: Source/toolchain behavior.** Rust source build replaces the Python ≥3.11 runtime requirement with a documented stable Rust toolchain while retaining a single `codex-meter` executable and no need for Python at runtime. Authority for existing behavior: `pyproject.toml`, `docs/build-from-source.md`. Rust verification: clean build/container jobs with Python absent.
- [ ] **TODO — X-003: Native Windows console.** Use UTF-8 input/output, native arrow/key handling and ANSI/VT where supported; redirected output remains valid UTF-8; do not require POSIX termios/select. Authority: `cli.py:_configure_windows_stdio`; `interactive.py` Windows branches. Rust verification: Windows unit tests and interactive smoke recording.
- [ ] **TODO — X-004: POSIX terminals.** Linux/macOS/WSL use raw/cbreak-compatible terminal input and restore it reliably; WSL reads the distribution-local Codex home. Authority: `interactive.py` POSIX branches; `docs/build-from-source.md`. Rust verification: Linux/macOS PTY suites and WSL documented smoke checklist.
- [ ] **TODO — X-005: Optional system tools degrade safely.** Core history/dashboard work without `ssh`, `tcpdump`, or `openssl`; their specific features provide actionable errors. Remote discovery supports GNU/BSD `stat`; tcpdump interface choice supports Linux/macOS. Authority: `remote.py`, `network.py`, `proxy.py`, `doctor.py`. Rust verification: PATH-stripped tests on Linux/macOS/Windows.
- [ ] **TODO — X-006: Installer contract.** One-line Unix and PowerShell installers select the correct asset, download `SHA256SUMS`, verify SHA-256 before install, install per user without sudo/Admin, add/report PATH appropriately, and never alter `~/.codex-meter` history. Authority: `install.sh`, `install.ps1`. Rust verification: release CI local-base-URL smoke tests and pre/post state hash.
- [ ] **TODO — X-007: Release contents.** Tagged releases include the four standalone executables and checksums; Rust packaging must replace or intentionally supersede the Python wheel while documentation and installers reference the new version/assets consistently. Authority: `.github/workflows/release.yml`, README install section. Rust verification: release-dry-run artifact manifest and installer tests against it.
- [ ] **TODO — X-008: Performance baseline.** Preserve immediate dashboard first paint and improve rather than regress normal command startup/import memory. Authority: v0.14.2/v0.15.0 changelog and `_interactive_dashboard`. Rust verification: reproducible benchmark for `--version`, `demo`, empty `today`, unchanged-history startup, first import and remote streaming, with Python v0.15.0 recorded as baseline.

## Required acceptance test matrix

- [ ] **TODO — T-001: Port all existing Python tests.** Recreate the behavioral coverage of every test in `tests/test_collector.py`, `test_pricing.py`, `test_identity_periods.py`, `test_quota.py`, `test_remote.py`, `test_live_observability.py`, `test_tui.py`, and `test_doctor.py`. Authority: `tests/`. Rust verification: mapped test inventory showing one or more Rust tests for every Python test name.
- [ ] **TODO — T-002: Golden CLI suite.** Snapshot global/nested help, normal reports, empty states, warnings, errors and exit codes for every command listed above. Authority: `cli.py`. Rust verification: golden tests on Linux, with portable assertions rerun on macOS/Windows.
- [ ] **TODO — T-003: Golden normalized database.** Feed identical source fixtures to fresh Python and Rust databases; compare schemas, table contents (excluding generated row IDs/timestamps where appropriate), aggregate query results and exports. Authority: collectors/storage/migrations. Rust verification: differential CI job.
- [ ] **TODO — T-004: Existing database upgrade.** Exercise a copy of a real/sanitized v0.15.0 database including WAL-era data, remote source paths, unpriced rows, account labels, OTLP points and network flows. Authority: compatibility goal P-002. Rust verification: integrity check plus all command smoke tests and no lost rows.
- [ ] **TODO — T-005: Privacy adversarial suite.** Place distinct canary secrets in Rollout messages/reasoning/tool output, App Server payloads, OTLP bodies/arbitrary attributes/headers, proxy bodies/headers/query strings, SSH streams and proxy credentials. Authority: P-003 and all privacy-filter code. Rust verification: scan database, WAL, SHM, Meter home, temp directory and captured stdout/stderr for forbidden canaries (allow only explicitly sent transparent proxy output, never persisted output).
- [ ] **TODO — T-006: Cross-platform CI.** Run format/lint/unit/integration/CLI smoke tests on Linux, macOS arm64/x86_64 and Windows x86_64; gate merge/release on all supported targets. Authority: current `.github/workflows/ci.yml` and release matrix. Rust verification: required green checks for the Rust branch.
- [ ] **TODO — T-007: Interactive PTY suite.** Verify navigation, palette, filtering, resize/short-terminal clipping, background quota/remote updates, refresh, every exit route and terminal restoration. Authority: `interactive.py`, `tests/test_tui.py`. Rust verification: automated PTY tests on Linux/macOS plus Windows console-state tests.
- [ ] **TODO — T-008: Real protocol smoke tests.** In addition to fakes, verify one real Codex Rollout import, one installed-Codex quota read when available, one OpenSSH alias sync, one OTLP HTTP submission, one App Server proxy exchange, one socket probe and local proxy/TLS loopback. Authority: source adapters and README workflows. Rust verification: documented release-candidate checklist with redacted evidence; unavailable external account/host checks must be explicitly recorded rather than silently skipped.

## Completion gate

- [ ] **TODO — G-001: Checklist closure.** Every item above is either verified complete with linked test/evidence or explicitly superseded by an approved product decision; no item may be omitted because it is difficult or platform-specific.
- [ ] **TODO — G-002: Python feature parity audit.** Re-run repository-wide command/function/schema discovery against the final Python v0.15.0 reference and reconcile any newly found behavior into this document and the Rust implementation.
- [ ] **TODO — G-003: User workflow audit.** Validate install → first launch → local history → day/week/month/all → quota → project filter → account opt-in → remote add/sync → Network → export → update, from the perspective of a non-engineer Codex user.
- [ ] **TODO — G-004: Release readiness.** Only after parity, compatibility, privacy, platform, performance and installer gates pass may the Rust rewrite replace the Python implementation on the default branch or receive a stable release tag.
